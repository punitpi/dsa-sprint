/* eslint-disable no-console */
const fs = require('fs')
const input = fs.readFileSync(0, 'utf8').trim().split(/\s+/)
let idx = 0
const next = () => input[idx++]

// ---------- solve ----------
function solve () {
  // write logic
}

solve()
